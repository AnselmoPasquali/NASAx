<!-- item_favorite.xml -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="8dp"
    app:cardBackgroundColor="#1A1A2E">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/ivFavoriteImage"
            android:layout_width="match_parent"
            android:layout_height="160dp"
            android:scaleType="centerCrop"
            android:transitionName="favorite_image"
            android:contentDescription="Foto preferita" />

        <!-- Gradiente per leggibilità testo -->
        <View
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_gravity="bottom"
            android:background="@drawable/gradient_top_bottom" />

        <!-- Titolo sovrapposto all'immagine in basso -->
        <TextView
            android:id="@+id/tvFavoriteTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:padding="8dp"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:fontFamily="@font/nasa_font"
            android:maxLines="2"
            android:ellipsize="end" />

        <!-- Badge "▶ Video" centrato — visibile solo per i video -->
        <TextView
            android:id="@+id/tvVideoLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="#CC000000"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:text="@string/video_badge"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <!-- Pulsante elimina (cestino) in alto a destra -->
        <ImageView
            android:id="@+id/btnDelete"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="top|end"
            android:layout_margin="6dp"
            android:background="#CC000000"
            android:padding="4dp"
            android:src="@drawable/ic_trash"
            android:contentDescription="Rimuovi dai preferiti"
            android:clickable="true"
            android:focusable="true" />

    </FrameLayout>

</com.google.android.material.card.MaterialCardView>